#ifndef DEADLIGHT_IMAPS_H
#define DEADLIGHT_IMAPS_H

#include "../core/deadlight.h"

// Public function to register the IMAP handler with the core system.
void deadlight_register_imaps_handler(void);

#endif // DEADLIGHT_IMAPS_H