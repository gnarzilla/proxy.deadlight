#ifndef DEADLIGHT_IMAP_H
#define DEADLIGHT_IMAP_H

#include "../core/deadlight.h"

// Public function to register the IMAP handler with the core system.
void deadlight_register_imap_handler(void);

#endif // DEADLIGHT_IMAP_H